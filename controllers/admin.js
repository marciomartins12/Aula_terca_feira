const produtos = require('../models/produtos');

module.exports = class AdminController {
    static async homeAdmin(req, res){
        res.render(<>esperando o front agora pra desenvolveer o resto</>)
    }
}